{
  "scripts": {
    "electron:dev": "cross-env ELECTRON_DEV=1 concurrently \"npm --prefix ../web run dev\" \"wait-on http://localhost:5173 && electron .\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "wait-on": "^7.0.1",
    "electron": "^28.0.0"
  }
}
